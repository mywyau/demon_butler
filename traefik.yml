api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":80"

providers:
  docker:
    endpoint: "unix:///var/run/docker.sock"
    exposedByDefault: false

http:
  middlewares:
    shared-cookie-headers:
      headers:
        customRequestHeaders:
          X-Cookie: "authToken"